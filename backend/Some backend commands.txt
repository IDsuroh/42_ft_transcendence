curl -X POST http://localhost:8000/api/users/signup/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "playern",
    "email": "playern@example.com",
    "password": "VeryStrongPassword123!"
  }'
Testing signup curl command.

docker compose exec db psql -U django -d transcendence_database
Enter transcendence_database

SELECT id, username, email, is_staff, is_superuser FROM users_user;
inspect user info

DELETE FROM users_user WHERE username = 'playern';
delete player number n

\q
Exit PostgreSQL

docker compose exec backend python manage.py createsuperuser
Create admin super user

docker compose exec backend python manage.py migrate
Migration files are only instructions.
If this is not run, the code may exist, but the database tables may not
Django can fail with errors like:
- relation "users_user" does not exist
- no such table
- database table missing