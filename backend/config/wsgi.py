"""
WSGI config for config project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings')

application = get_wsgi_application()
# Creates the actual Django WSGI application object. WSGI (Web Server Gateway Interface) standard connection format that lets Gunicorn call a Django application.
# Its job is to expose the Django application to Gunicorn.